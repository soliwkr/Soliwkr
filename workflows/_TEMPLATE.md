# Workflow: <Nome del workflow>

> SOP template. Copia questo file e rinominalo (es. `scrape_website.md`).
> Scrivi in linguaggio chiaro, come faresti per un membro del team.

## Objective
Che cosa deve ottenere questo workflow, in una o due frasi.

## Trigger
Quando eseguire questo workflow (richiesta dell'utente, evento, pianificazione).

## Required Inputs
- `input_1` — descrizione, formato, dove reperirlo
- `input_2` — ...

## Tools Used
- `tools/<script>.py` — cosa fa e come invocarlo
- Elenca in ordine di esecuzione

## Steps
1. Passo con il tool da usare e gli input attesi
2. ...
3. Output prodotto

## Expected Outputs
Dove finisce il risultato (servizio cloud, file in `.tmp/`, ecc.) e in che formato.

## Edge Cases & Failure Handling
- Errore atteso → come reagire
- Rate limit / retry → strategia
- Input mancante → cosa chiedere all'utente

## Notes / Learnings
Vincoli scoperti, quirk delle API, timing. Aggiornare man mano che si impara.

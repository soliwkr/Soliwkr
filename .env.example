# =============================================================
# .env.example — copy to .env and fill in real values.
# NEVER commit the real .env. Secrets live ONLY here.
# =============================================================

# --- LLM / AI ---
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# --- Google (OAuth) ---
# credentials.json + token.json handle Google auth (gitignored).
# Add any extra Google config below as needed.
GOOGLE_APPLICATION_CREDENTIALS=credentials.json

# --- Add per-tool API keys below as workflows require them ---
# EXAMPLE_SERVICE_API_KEY=
